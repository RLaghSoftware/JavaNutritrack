# Copy to .env in project root (never commit .env)

# MySQL (Workbench root @ localhost:3306, schema nutritrackjava)
MYSQL_PASSWORD=your_mysql_root_password

# JWT signing secret — use a long random string (32+ characters)
JWT_SECRET=change_me_to_a_long_random_secret_key_at_least_32_chars
